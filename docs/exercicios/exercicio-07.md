# Exercício 07 - Git e GitHub

## 🛠 Questões Práticas

**1. A Metáfora do Save Point**
Explique a diferença entre `git add` (preparar a mochila) e `git commit` (tirar a foto/salvar o jogo). Por que não fazemos tudo num comando só?

**2. Detetive de Código**
Se um bug apareceu no sistema hoje, mas ontem estava tudo funcionando, como o comando `git log` (que mostra o histórico) pode ajudar a descobrir quem causou o problema e onde?

**3. Trabalho em Grupo**
João e Maria estão mexendo no MESMO arquivo ao mesmo tempo.
- João deu `git push` primeiro.
- Maria tentou dar `git push` depois e recebeu um erro.
O que Maria precisa fazer antes de enviar o código dela? (Pesquise sobre `git pull` e resolução de conflitos).
